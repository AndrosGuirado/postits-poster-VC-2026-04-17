---
description: Run svelte-check + TypeScript type checks
---

Run `pnpm check` and report the result. If there are errors, group them by file and list each error's line and message; do not attempt fixes unless the user follows up asking for them.
