---
description: Run a production build via Vite
---

Run `pnpm build`. On success, report bundle output paths and any warnings. On failure, surface the first build error verbatim so the user can act on it.
