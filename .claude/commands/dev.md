---
description: Start the Vite dev server for Postits
---

Run `pnpm dev` in the project root and report the local URL once Vite reports it as ready. If port 5173 is already in use, surface the conflict instead of silently moving to another port.
