---
description: Preview the production build locally
---

Run `pnpm preview` after confirming a build exists in the SvelteKit output directory. If no build exists, run `pnpm build` first, then preview.
